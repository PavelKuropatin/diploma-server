package by.bntu.diploma.diagram.repository;

import by.bntu.diploma.diagram.entity.EndpointStyle;
import org.springframework.data.jpa.repository.JpaRepository;

public interface EndpointStyleRepository extends JpaRepository<EndpointStyle, Long> {
}
